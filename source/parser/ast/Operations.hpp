#ifndef REMLANG_OPERATIONS_HPP
#define REMLANG_OPERATIONS_HPP

enum class Operations {
    PLUS,
    MINUS,
    MULTIPLY,
    DIVISION,

    OR,
    AND,

    EQUAL,
    NOTEQUAL,
    MIN,
    MAX,
    MINEQUAL,
    MAXEQUAL,
};

#endif